typ character = char;
var firstChar : character;
var charArray : [10]char;
var counter : int;

fun main() : void = {
    counter = (0 + 1 - 1) * 256;

    while (3 + 2 where var x : int;) do
        setChar(charArray^, counter, (counter + 2 * 1 - 3 / 5 % 2 + 48 : char));
        counter = counter + 1;
    end;

    printArr(charArray);

    x = 2 + 1 - 3 / 2 + 8[2];

    if (b != 3 | c < 2 | d > g & l >= k) then
        counter = 0;
    else
        counter = 1;
    end;

    (x where fun a() : void = {x = x + 1;};);
};

fun setChar(arr : [10]^char, count : int, val : char) : void = {
    if (val != 'a') then
        arr[count] = val;
    else
        arr[count] = '\\';
    end;
};

fun printArr(arr : [10]char) : void = {
    count = 0;
    while (count != 10) do
        putChar(arr[count]);
        count = count + 10;
    end;
};